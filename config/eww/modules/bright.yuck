(defwidget bright []
  (box
    :class "module"
    (eventbox
      :onscroll "echo {} | sed -e 's/up/-A 1/g' -e 's/down/-U 1/g' | xargs brillo"
      (label
        :text {brightness.icon}
        :class "bright-icon icon"
        :tooltip "brightness ${round(brightness.level, 0)}%"))))
