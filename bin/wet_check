#!/bin/bash
path="$HOME/.cache/wet.txt"
[[ -f "$path" ]] || echo "Klaipeda" > $path
location=$(cat $path)

case "$location" in
  "Kaunas") echo "Klaipeda" > $path
  ;;
  "Klaipeda") echo "Kaunas" > $path
  ;;
  *) echo "Klaipeda" > $path
  ;;
esac
pkill -SIGRTMIN+8 "waybar"



