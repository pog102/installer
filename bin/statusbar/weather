#!/bin/bash

# Deps:
# 	curl

source color
sou=$(curl -s "wttr.in/?format=1" | 
	sed "s/⛅/ /" |
	sed "s/☀/ /"
)
icon=$( echo $sou | awk '{print $1;}' )
temp=$( echo $sou | grep -Eo '[\+|\-].*°C' )
printf "%s %s %s%s  " "$bg1$fg0" "$icon" "$reset$fg1" " $temp"


